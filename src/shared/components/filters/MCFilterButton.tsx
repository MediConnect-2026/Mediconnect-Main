import { Button } from "@/shared/ui/button";

interface MCFilterButtonProps {
  children?: React.ReactNode;
  filtersCount?: number;
  variant?: "primary" | "secondary" | "outline";
}

function MCFilterButton() {
  return <div>MCFilterButton</div>;
}

export default MCFilterButton;
